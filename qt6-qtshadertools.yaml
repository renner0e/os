package:
  name: qt6-qtshadertools
  version: "6.9.1"
  epoch: 0
  description: Qt6 - Qt Shader Tools module builds on the SPIR-V Open Source Ecosystem
  copyright:
    - license: LGPL-3.0-only OR GPL-3.0-only WITH Qt-GPL-exception-1.0


environment:
  contents:
    packages:
      - build-base
      - busybox
      - clang
      - cmake
      - ninja
      - libglvnd-dev
      - python3-dev
      - qt6-qtbase-dev
      - libxkbcommon-dev

pipeline:
  - uses: git-checkout
    with:
      expected-commit: 661130571df1a19bd10dd7705fe689cfde835dad
      repository: https://code.qt.io/qt/qtshadertools.git
      tag: v${{package.version}}


  - uses: cmake/configure

  - uses: cmake/build

  - uses: cmake/install

  - uses: strip

update:
  enabled: true
  git:
    strip-prefix: v


subpackages:
  - name: ${{package.name}}-dev
    description: ${{package.name}} development libraries and headers
    pipeline:
      - uses: split/dev
    test:
      pipeline:
        - uses: test/tw/ldd-check
        - uses: test/pkgconf

